% RPM
% Gavin Beatty <gavinbeatty@gmail.com>

## Use $rpmroot instead of /usr for finding /usr/src/redhat/...

    rpmbuild --define="_usr $rpmroot" ...

## Extract the rpm into the current directory

    rpm2cpio some.rpm | cpio -idmv

